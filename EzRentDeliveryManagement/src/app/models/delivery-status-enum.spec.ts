import { DeliveryStatusEnum } from './delivery-status-enum';

describe('DeliveryStatusEnum', () => {
  it('should create an instance', () => {
    expect(new DeliveryStatusEnum()).toBeTruthy();
  });
});
